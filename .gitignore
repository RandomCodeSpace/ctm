# Built binary
ctm

# Go coverage / profiling
*.out
*.test
coverage.*

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
.superpowers/

# Planning / spec docs — kept locally, not shared in repo history
docs/

# Subagent worktree scratch paths (managed by the harness, not source)
.claude/worktrees/

# Local agent loop state (ralph-loop iteration snapshots, etc.) — runtime, not source
.claude/*.local.md

# Local code-intelligence cache (codeiq) — runtime DB + neo4j store, not source
.codeiq/

# Coverage reports — regenerated per-test, never committed
coverage.out
