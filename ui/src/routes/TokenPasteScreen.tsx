// Re-export so the /auth route can render the same component used as a
// fallback inside <AuthProvider>. The component owns its own form state.
export { TokenPasteScreen } from "@/components/TokenPasteScreen";
