package hermes

// BuildCommand is implemented in Task 2 (see plan).
// Stub returns empty so the package compiles for Task 1's registration tests.
func BuildCommand(agentSessionID, mode string, resume bool, envExports string) string {
	return ""
}
